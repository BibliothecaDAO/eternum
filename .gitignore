.worktrees/
node_modules
target
.snfoundry_cache
.wrangler
docs/dist
.DS_Store
**/.DS_Store
.aider*
ADW/
unoptimized-*/
CLAUDE.local.md
.env.local
venv/
__pycache__/
apps/game/minimap
.claude/ralph-loop.local.md
.claude/tsc-cache/
**/.claude/tsc-cache/
.claude/worktrees/
**/.claude/worktrees/
.claude/cache/
**/.claude/cache/
thoughts/

# launcher run-store artifacts (config/deployer/clean/launch/*-io.ts)
.context/
.playwright-mcp/

# ATHANOR generated state (world address, rendered configuration, run manifests)
deploy/athanor/.lab/

# Local lab configuration for apps/web and packages/db (read via `pnpm with-env`); never committed.
.env

# Local animation captures and benchmark reports
/output/
.gstack/

# Agent runner data dirs (guest keys, memory, run manifests); never committed.
.agent-data/
