mod config {
    mod contracts;
    // #[cfg(test)]
    // mod tests;
}
mod realm {
    mod contracts;
    // #[cfg(test)]
    // mod tests;
}
mod trade {
    mod contracts;
    // #[cfg(test)]
    // mod tests;
}
mod resources {
    mod contracts;
    // #[cfg(test)]
    // mod tests;
}
mod transport {
    mod contracts;
    // #[cfg(test)]
    // mod tests;
}

mod leveling {
    mod contracts;
    mod interface;
    // #[cfg(test)]
    // mod tests;
}
mod name {
    mod contracts;
}
mod hyperstructure {
    mod contracts;
    mod interface;
    // #[cfg(test)]
    // mod tests;
}
mod buildings {
    mod contracts;
    // #[cfg(test)]
    // mod tests;
}
mod map {
    mod contracts;
    // #[cfg(test)]
    // mod tests;
}
mod dev {
    mod contracts;
}
mod combat {
    mod contracts;
    // #[cfg(test)]
    // mod tests;
}

mod bank {
    mod contracts;
    #[cfg(test)]
    mod tests;
}

