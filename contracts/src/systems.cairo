mod config {
    mod contracts;
    mod interface;
    #[cfg(test)]
    mod tests;
}
mod realm {
    mod contracts;
    mod interface;
    #[cfg(test)]
    mod tests;
}
mod trade {
    mod contracts;
    mod interface;
    #[cfg(test)]
    mod tests;
}
mod resources {
    mod contracts;
    mod interface;
    #[cfg(test)]
    mod tests;
}
mod transport {
    mod contracts;
    mod interface;
    #[cfg(test)]
    mod tests;
}
mod combat {
    mod contracts;
    mod interface;
    #[cfg(test)]
    mod tests;
}
mod leveling {
    mod contracts;
    mod interface;
    #[cfg(test)]
    mod tests;
}
mod name {
    mod contracts;
}
mod hyperstructure {
    mod contracts;
    mod interface;
    #[cfg(test)]
    mod tests;
}
mod buildings {
    mod contracts;
    #[cfg(test)]
    mod tests;
}
mod map {
    mod contracts;
    #[cfg(test)]
    mod tests;
}
mod test {
    mod contracts;
    mod interface;
}
mod combatv2 {
    mod contracts;
    mod interface;
    #[cfg(test)]
    mod tests;
}

mod bank {
    mod contracts;
    mod interface;
    #[cfg(test)]
    mod tests;
}

