mod config {
    mod contracts;
    mod interface;
    #[cfg(test)]
    mod tests;
}
mod realm {
    mod contracts;
    mod interface;
    #[cfg(test)]
    mod tests;
}
mod labor {
    mod contracts;
    mod interface;
    #[cfg(test)]
    mod tests;
    mod utils;
}
mod trade {
    mod contracts;
    mod interface;
    #[cfg(test)]
    mod tests;
}
mod resources {
    mod contracts;
    mod interface;
    #[cfg(test)]
    mod tests;
}
mod transport {
    mod contracts;
    mod interface;
    #[cfg(test)]
    mod tests;
}
mod combat {
    mod contracts;
    mod interface;
    #[cfg(test)]
    mod tests;
}
mod leveling {
    mod contracts;
    mod interface;
    #[cfg(test)]
    mod tests;
}
mod name {
    mod contracts;
    mod interface;
    #[cfg(test)]
    mod tests;
}
mod hyperstructure {
    mod contracts;
    mod interface;
    #[cfg(test)]
    mod tests;
}
mod buildings {
    mod contracts;
    mod interface;
    #[cfg(test)]
    mod tests;
}
mod map {
    mod contracts;
    mod interface;
    #[cfg(test)]
    mod tests;
}
mod test {
    mod contracts;
    mod interface;
}
mod bank {
    mod contracts;
    mod interface;
    #[cfg(test)]
    mod tests;
}

