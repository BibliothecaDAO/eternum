query getMyOffers($makerId: u128!) {
  tradeComponents(
    maker_id: $makerId
    claimed_by_maker: 0
    claimed_by_taker: 0
  ) {
    entity {
      keys
      components {
        __typename
        ... on Status {
          value
        }
        ...TradeFragment
      }
    }
  }
}
