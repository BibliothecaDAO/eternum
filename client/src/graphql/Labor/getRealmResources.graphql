query getRealmResources($realmEntityId: String!) {
  entities(keys: [$realmEntityId]) {
    edges {
      node {
        keys
        components {
          __typename
          ... on Resource {
            balance
          }
        }
      }
    }
  }
}
