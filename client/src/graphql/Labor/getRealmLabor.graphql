query getRealmLabor($realmEntityId: String!) {
  entities(keys: [$realmEntityId]) {
    edges {
      node {
        keys
        components {
          __typename
          ... on Labor {
            last_harvest
            balance
            multiplier
          }
        }
      }
    }
  }
}
