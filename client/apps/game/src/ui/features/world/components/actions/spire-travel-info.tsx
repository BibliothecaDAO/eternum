import { InfoLabel } from "./info-label";

export const SpireTravelInfo = () => {
  return (
    <InfoLabel variant="default" className="items-center justify-center gap-1 text-xxs uppercase tracking-[0.2em]">
      Traverse the spire gateway
    </InfoLabel>
  );
};
