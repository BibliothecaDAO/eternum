{
	auto_https off
	admin off
}

:8080 {
	encode zstd gzip

	handle /api/* {
		respond "API routes are served by the staging Workers" 404
	}

	handle {
		root * /srv
		try_files {path} /index.html
		file_server
	}
}
