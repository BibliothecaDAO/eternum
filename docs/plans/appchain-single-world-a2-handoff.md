# A2 — Launch Pipeline Collapse — Codex Handoff

Status: **CODE ACCEPTED 2026-08-09 (rounds 1-2 complete, commit 16504c985b; deployment leg in progress).** Milestone: A2
of `docs/plans/appchain-single-world.md`. Prereq: A1 ACCEPTED (registrar world on `feat/single-world-blitz`, 181/181
tests).

**Motto: KISS.** Every deliverable below is the smallest workable version. Do not add abstraction layers, config
frameworks, optional features, or refactors beyond what is listed. When two designs are close, pick the simpler one.
Overcomplication is a review finding, same as a bug.

## Mission

Today an appchain Blitz launch is: factory `create_game` ×~10 batched txs (~2.5 min) → poll `wf-WorldDeployed` →
~146-call config multicall → reserve-hyperstructures step → per-game role grant → torii indexer registration. Total ≈ 10
min. After A1 the world is persistent and `registrar.create_game` does everything per-game in ONE call (config copy,
hyperstructure ring reservation included). Collapse the pipeline so a UI-button launch reaches a joinable game in **< 30
s**, while the launch service Lambda and factory-v2 UI keep their exact contracts.

## Ground rules

- Branch: `feat/single-world-blitz`. NEVER commit to `feat/appchain-phase-1`.
- Scope: `config/` (deployer + source + generated regen), `.github/workflows/game-launch.yml`,
  `deploy/appchain/scripts/`, `contracts/l3/game/dojo_appchain.toml`. Do NOT touch: `client/` (A4), torii fork (A3),
  `deploy/appchain/cdk/` (Lambda/CDK are the reviewer's deploy domain — the Lambda contract needs zero changes),
  `contracts/l3/factory/` (retired in place), `packages/provider` / `packages/types` (build new calls with plain
  starknet.js + the world manifest ABI inside `config/deployer/clean/`, not by extending published packages).
- Cairo: A2 should need NO contract changes. If you hit a genuine registrar gap (e.g. `register_preset` calldata
  physically cannot fit one tx), STOP and record it in `docs/plans/A2-NOTES.md` for the reviewer — do not modify
  contracts unilaterally.
- **The `mainnet.blitz` / `mainnet.eternum` factory flow stays byte-for-byte functional.** The collapse applies to
  `appchain.*` environments only, behind a plain environment check. Dead appchain-only factory code paths: comment out
  with an A2 marker, don't delete.
- Toolchain: bun + pnpm as the pipeline already uses; local chain via `deploy/appchain/spike`
  (`docker-compose up -d --build`, katana on :5050; sozo 1.8.x via asdf).
- Accounts: `0x127f…cfcec` (the key currently sitting in `dojo_appchain.toml` / `constants.ts:41-44`) is the
  **paymaster's account — never use it in tooling**. All new scripts take `DOJO_ACCOUNT_ADDRESS` / `DOJO_PRIVATE_KEY`
  from env (the GH environment already provides them: workflow lines 144-145). Remove the hardcoded key from
  `dojo_appchain.toml` env in favor of env vars.

## The registrar API (already deployed logic — this is what the pipeline calls)

`contracts/l3/game/src/systems/registrar/contracts.cairo:44-63`:

- `bootstrap_chain_config(chain_config: ChainConfig)` — once per world; caller must be the Dojo owner of
  `s2_blitz-registrar_systems` AND become the configured admin. Write-once.
- `register_preset(preset_config: PresetConfig, game_config: PresetGameConfig, side_tables: PresetSideTables)` —
  admin-only, write-once per `preset_id` (preset 0 reserved). `PresetSideTables` spans: weights, resource_factories,
  building_categories, structure_levels, hyperstructure_construction, resource_lists, resource_min_max_lists (all rows
  carry `preset_id`; the registrar stamps/validates it).
- `register_series(series_id, owner, num_games, total_chests, cap_ratio_bps)` — admin-only.
- `create_game(params: CreateGameParams) -> u32` — one tx per game. Params (contracts.cairo:20-41): name, preset_id,
  series_id + game_number_in_series (0/0 for standalone), start_settling_at, start_main_at, duration_seconds,
  end_grace_seconds, registration_grace_seconds, dev_mode_on, single_realm_mode, two_player_mode, registration_count_max
  (≤24), registration_start_at, fee_amount, biome_climate_config, use_map_override + map_override, seed (non-zero).
  Reserves the full hyperstructure ring inside this tx.
- The Cairo struct layouts are the calldata contract. The dispatcher test fixture
  (`contracts/l3/game/src/systems/registrar/tests.cairo`, `dispatcher_lifecycle` mod) shows a complete working
  `register_preset` + `create_game` invocation — mirror it.

## Deliverables

**D1 — registrar call module** (`config/deployer/clean/registrar/` — one file if it fits): plain starknet.js
`Account.execute` wrappers for the four entrypoints above, ABI/address resolved from
`contracts/l3/game/manifest_appchain.json` (same pattern as `deploy/appchain/scripts/*.ts`). No batching machinery, no
provider-package changes.

**D2 — preset builder**: a function mapping the EXISTING resolved config artifact
(`config/generated/blitz.appchain.json`, regenerated by `pnpm run config:sync:appchain` — keep this as the single source
of truth, invent no new format) → the `register_preset` calldata. The mapping mirrors what the 29 config steps
(`config/deployer/clean/config/steps.ts:110-147` + `native-steps.ts`) write today, re-bucketed per audit §3: static
rulebook members → `PresetConfig` + side tables; per-game members → `PresetGameConfig` template; chain addresses →
`ChainConfig`. Where a value is per-game (clock, fees, biome, map override, seed) it goes in `CreateGameParams` at
launch time, not the preset. CLI: `register-preset.ts --preset-id <n> [--dry-run]` (dry-run prints calldata + counts).

**D3 — world-deploy runbook script** (`deploy/appchain/scripts/deploy-s2-world.ts` + a short README section):
sequential, idempotent-where-possible steps the reviewer runs once per chain:

1. `sozo build && sozo migrate --profile appchain` (document the command; migration itself is run by the reviewer) —
   profile updated per D6.
2. Wire shared peripherals: set entry-NFT minter = `s2_blitz-blitz_realm_systems` address, loot-chest minter =
   `s2_blitz-prize_distribution_systems` address (reuse the grant-role call shapes from the existing grant
   workflows/scripts).
3. `bootstrap_chain_config` — addresses read from the same resolved config JSON (chain section) + env overrides; skip
   with a clear message if ChainConfig is already initialized.
4. `register-preset.ts --preset-id 1`; skip-if-registered.

**D4 — launch runner collapse** (`config/deployer/clean/launch/runner.ts` + `series-runner.ts`), appchain path only:

- `create-world` step → ONE `registrar.create_game` call built from the same launch inputs that feed the factory path
  today (`GAME_LAUNCH_*` env: name, start time, durationSeconds, devModeOn, twoPlayerMode, singleRealmMode,
  mapConfigOverrides, biomeClimateOverrides, blitzRegistrationOverrides). `version` maps to `preset_id` (the Lambda/UI
  keep sending `version` — parse it as the preset id, default 1). Record `createGameTxHash` in run-record artifacts plus
  ONE new artifacts key `gameId` (the block is extensible JSON; the UI ignores unknown keys). Delete the
  batching/submission-count/retry machinery from this path (single tx; keep one simple retry-on-timeout). Keep the
  account lease for this step only.
- `wait-for-factory-index` step (id unchanged, title "Waiting for game") → poll torii SQL for the
  `s2_blitz-GameRegistry` row with that game_id (2 s poll, 2 min timeout). This doubles as the indexing check.
- `configure-world`, `reserve-blitz-hyperstructures`, `grant-lootchest-role`, `create-indexer`, `sync-paymaster` → do
  not run and do not emit on the appchain path (run-record `steps[]` is data-driven; the UI renders what's present).
  Mainnet path emits exactly what it does today.
- Series (`create-series`) → one `register_series` call; per-game launches then pass series_id + game_number. Rotation
  logic sits on top unchanged.

**D5 — `game-launch.yml`**: gate the now-dead steps to `mainnet.*` environments (extend the existing
`endsWith(inputs.environment, '.blitz')`-style conditions); leave step order, run-record events, artifact upload, and
the lease helpers as they are. No new steps.

**D6 — appchain profile** (`contracts/l3/game/dojo_appchain.toml`): namespace `default = "s2_blitz"`, new `[world] seed`
(`"s2_blitz_aws_1"`), writers list = the compiled `s2_blitz-*_systems` (mirror `dojo_local.toml`'s list post-A1, incl.
`registrar_systems`), `[env]` keeps rpc_url but account/key come from env (delete the hardcoded paymaster credentials).
`manifest_appchain.json` gets regenerated when the reviewer migrates — code against the manifest structure produced by
your local spike migration.

## What dies vs. survives (appchain path)

| Today                                                        | After A2                                       |
| ------------------------------------------------------------ | ---------------------------------------------- |
| factory `create_game` ×10 batched txs + 15×5 retry machinery | one `registrar.create_game` tx                 |
| `wf-WorldDeployed` torii poll (5 min budget)                 | `s2_blitz-GameRegistry` row poll (seconds)     |
| 29-step / ~146-call config multicall per game                | dies — preset registered once per chain        |
| reserve-blitz-hyperstructures step                           | dies — inside `create_game`                    |
| grant-lootchest-role per game                                | dies — one-time in D3                          |
| create-indexer (15 min budget)                               | dies — world always indexed                    |
| `max_actions` ceiling, config versions                       | obsolete — `version` input now means preset_id |
| account lease, run-record events, Lambda, UI                 | unchanged                                      |

## Facts that save time

- Workflow anatomy: single `launch` job, steps at `game-launch.yml:148-1010`; shell library generated at step "Prepare
  launch arguments" (:173-794); `run_launch_step` → `bun config/deployer/clean/cli/launch-step.ts --step <id>`;
  run-record events via `launch-run-store.ts`.
- Run store = GitHub branch `factory-runs`, written via Contents API (`config/deployer/clean/run-store/github.ts`); step
  ids in `config/deployer/clean/types.ts:16-25`; record schema `run-store/types.ts:196-217`. Keep ids/schema; emit a
  subset.
- Step registry comment (`config/deployer/clean/config/steps.ts:108-109`) calls itself "the contract between CI, the
  admin UI, and the onchain systems" — that contract is now scoped to mainnet.
- `config:sync:appchain` regenerates `config/generated/blitz.appchain.json` from `config/source/blitz/*`; the launcher
  loads it via `config-loader.ts:421-424`. Reuse; don't fork.
- Torii SQL base URL comes from `common/factory/endpoints.ts` / `TORII_URL` env (workflow :142).
- `sync-paymaster` is already a no-op on the appchain (embedded paymaster — `deploy/appchain/spike/README.md:154-162`).
- The Lambda (`deploy/appchain/cdk/lambda/launch-service/index.mjs`) dispatches with `ref: DEFAULT_WORKFLOW_REF` — it
  will point at this branch only when the reviewer flips it. You cannot break production from here.
- Local e2e: `deploy/appchain/spike/docker-compose.up` → migrate with a spike-rendered profile (see
  `spike/scripts/deploy-worlds.sh` for the render pattern) → run D3 → run the D4 create-game step via `launch-step.ts`
  pointed at localhost. Torii is optional locally (fork image); if you skip it, verify the GameRegistry row via a direct
  RPC `call` instead and note it.

## Definition of done

1. Local spike run: deploy-s2-world (D3) succeeds; then one launch-step create-game + wait completes with a
   Live/Registration `GameRegistry` row — **wall clock < 30 s** for the per-game part; record the timing in
   `docs/plans/A2-NOTES.md`.
2. `--dry-run` of `register-preset.ts` prints a stable calldata summary (counts per side table) — include it in
   A2-NOTES.
3. The mainnet path is behaviorally untouched: `git diff` on the mainnet-reachable code paths is empty-or-comments;
   state this explicitly in A2-NOTES.
4. `pnpm run format` and `pnpm run knip` pass; workflow YAML parses (`actionlint` if available, else careful review).
5. `docs/plans/A2-NOTES.md`: decisions, timings, dry-run summary, anything escalated.
6. Reviewer (Claude) then: reviews the diff, deploys the s2 world to the dev appchain with D3, registers the preset,
   flips the torii registry + Lambda `DEFAULT_WORKFLOW_REF`, and runs a real UI-button launch.

---

## Round 2 addendum (review of 11100ae4c8, 2026-08-09)

Round-1 verdict: the collapse is architecturally right and remarkably clean — full TS→Cairo serde audit found ZERO
field-order/encoding defects across all struct families; the legacy-step mapping table is complete
(hyperstructure_construction=0 confirmed byte-identical to legacy); mainnet classified hunk-by-hunk with ZERO behavior
changes; all touched tests pass (56/56 per-file; whole-dir failures are pre-existing `mock.module("starknet")`
cross-file leakage, not yours); D6 writers byte-identical to the validated spike profile; spike evidence reproduced. TWO
blockers and a short fix list before acceptance:

**R2-1 (BLOCKER) — every real UI/Lambda launch sends preset_id 140 and reverts.** `cli/launch-request.ts:549` fills
`version: args.version || DEFAULT_VERSION` ("140") BEFORE the runner runs, so the appchain default at `runner.ts:180` is
dead code on the production path (the Lambda never sends `version`; the workflow only passes `--version` when
`GAME_LAUNCH_VERSION` is set). `create_game` with preset 140 reverts on "Eternum: preset is not registered". Fix in ONE
place: make the CLI resolver's default chain-aware (`args.version || (chain === "appchain" ? "1" : DEFAULT_VERSION)`)
and DELETE the runner-side fallback. Add a test at the CLI-resolution boundary: appchain environment + no version → "1"
(the existing tests call the runner directly, which is why they couldn't catch this).

**R2-2 (BLOCKER) — deploy-s2-world grants roles before the registrar guard, against a possibly-stale manifest.**
`deploy-s2-world.ts:126-141` runs `wireSharedCollectibles` FIRST; with the tracked (still pre-A1 s1)
`manifest_appchain.json` it resolves s1 contract addresses via the namespace-blind suffix match at `calls.ts:50` and
submits two real grant txs to dead contracts before `requireRegistrarContract` finally throws. Fix: (a) run the
registrar guard/bootstrap resolution BEFORE any tx; (b) match manifest tags with the `s2_blitz-` prefix (as
`game-registry.ts:13-15` already does), so a stale manifest fails fast everywhere, including `--dry-run` and
`resolveAppchainWorldAddress`.

**R2-3 — duplicate-game protection.** `create_game` has no on-chain name-uniqueness; the skip check
(`game-registry.ts:84`) scans only the newest 50 rows and `.catch(() => null)` turns a torii outage into a blind resend
(workflow retries create-world with max_attempts=2 → duplicate game). Fix: query by name with a WHERE clause (drop the
LIMIT-50 client scan), and FAIL CLOSED — if the existence check errors, abort the step with a clear message instead of
resending.

**R2-4 — poll defaults.** Same shadowing pattern as R2-1: the CLI always fills `waitForFactoryIndexPollMs/TimeoutMs`
with the factory defaults (5 s / 5 min), so the runner's 2 s / 2 min never apply. Make the CLI defaults chain-aware
(appchain 2 s / 2 min); mainnet unchanged.

**R2-5 — end_grace_seconds: explicit, not borrowed.** The preset builder derives the default from
`bridgeCloseAfterEndSeconds` (604 800 s = 7 days), which keeps every season-gated entrypoint open and blocks the escrow
sweep for a week after each 2-hour game. Owner-signed ruling: add an explicit `endGraceSeconds` to the blitz source
config with default **86 400** (24 h claim window), per-launch override unchanged.

**R2-6 — golden calldata snapshot.** `registrar-preset.test.ts` pins only counts + total length (2 037), so a same-width
field swap passes every test. Add ONE snapshot of the full felt array for preset 1 (and one for a `CreateGameParams`
build) — the cheap durable guard for the whole serde class.

**R2-7 (mechanical)** — finish the `stepId` narrowing fix in `run-store/series-store.ts` and
`run-store/rotation-store.ts` (same one-liner already applied in `run-store/store.ts`).

**R2-8 (markers)** — the handoff asked for A2 retirement markers on dead appchain factory paths: add a one-line header
comment to `deploy/appchain/scripts/{factory-config,factory-create-game,blitz-flow}.ts` ("retired by A2 — appchain
launches go through the registrar; kept for mainnet-era reference").

**Record in A2-NOTES (no code):** series `num_games` is frozen at first creation and `create_game` for a series must
come from the series owner account — deployer key rotation mid-series bricks appends (accepted limitation); the
deterministic seed (poseidon of public inputs) is acceptable because combat randomness stays VRF-backed;
`appchain.eternum` launches intentionally reject; all appchain games share one `worldAddress` (A4 UI note, plus
surfacing `artifacts.gameId`); whole-dir `bun test` has pre-existing cross-file mock leakage — run per-file.

Definition of done for this round: R2-1..R2-8 landed; touched tests green per-file; a fresh local spike create-world run
driven through the REAL CLI path (`launch-step.ts` with no --version) proving preset 1 resolves; A2-NOTES updated.
Reviewer then re-audits and proceeds to the dev-appchain deployment.
