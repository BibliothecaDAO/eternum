{
  "name": "@bibliothecadao/operator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "start": "bun run src/server.ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@bibliothecadao/eternum": "workspace:*",
    "pg": "^8.18.0",
    "starknet": "catalog:web"
  },
  "devDependencies": {
    "@types/node": "^22.13.5",
    "@types/pg": "^8.16.0",
    "bun-types": "^1.2.2",
    "typescript": "^5.8.2",
    "vitest": "^2.1.9"
  }
}
